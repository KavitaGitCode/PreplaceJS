consider 1st element as minimum, to set the pointer
and compare each element in array and if find out is pointer (which conside to be the first element)
is greater than other element then this other element consider as pointer
goes to the end of array length
and once it goes to tthe end it swap the elmenet with the element is is less than pointer 

2 iteration will happen
[432, 23, 2, 8, 39, 78];
taking 1st iteration eg 432 : 1st iteration
and the compare 432 with each element will be 2dn iteration

pointer aage badhega 1st iteration 432, fir 23...8 this will be your 1st iteraion
and the comparison we did that is inner loop  

selection sort mai compare baar baar hoga but swap one time hoga
but bubble sort mai comapare baar baar hoga and swap bhi

time complexcity : O(n^2) and a space complexity of O(1) 
O(n²) ... O(N^2) → Quadratic Time 
n squre

The time complexity of the selection sort is O(N2) as it itterate twice (use 2 loops) and the space complexity is O(1) as there is no extra memory space is required.